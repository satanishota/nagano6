<h1>本当に退会しますか？</h1>

<%= link_to '有効', edit_customer_path(@customer.id)%>

<%= form_with model:@customer, url:customers_withdraw_path(@customer.id), local:true do |f| %>

  <%= f.hidden_field :is_active, :value =>true %>

  <%= f.submit '退会',data: { confirm: '本当に退会しますか？' } %>

  <% end %>